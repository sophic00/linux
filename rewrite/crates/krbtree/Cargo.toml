[package]
name = "krbtree"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rust rewrite of the Linux kernel's lib/rbtree.c as an owning red-black tree map"

[dependencies]
