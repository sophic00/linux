[package]
name = "ktest-util"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared test helpers (multiset asserts, fuzz driver) for the kernel rewrite workspace"

[dependencies]
krand = { path = "../krand" }
