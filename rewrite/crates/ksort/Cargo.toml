[package]
name = "ksort"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rust rewrite of the Linux kernel's lib/sort.c (bottom-up heapsort) and lib/bsearch.c"

[dependencies]

[dev-dependencies]
# Workspace test RNG: replaces the hand-rolled xorshift/LCG closures.
krand = { path = "../krand" }
ktest-util = { path = "../ktest-util" }
